import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import clipperPlugin from './plugins/babel-plugin-clipper-log';

export default defineConfig({
  plugins: [
    react({
      babel: {
        plugins: [clipperPlugin],
      },
    }),
  ],
});
